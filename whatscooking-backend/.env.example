# Connection string do seu cluster MongoDB Atlas
# Atlas UI → Database → Connect → Drivers → copie a URI
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@<seu-cluster>.mongodb.net/whatscooking?appName=Cluster1

# Porta do backend (default: 5000)
PORT=5000
